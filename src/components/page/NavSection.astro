---
const { title } = Astro.props;
---

<style>
  .NavSection + .NavSection {
    margin-block-start: var(--space-lg);
  }

  .NavSection span {
    display: block;
  }

  .NavSection ul > :global(* + *) {
    margin-block-start: var(--space-xxs);
  }
</style>

<li class="NavSection">
  <span class="delta">{title}</span>
  <ul>
    <slot />
  </ul>
</li>
